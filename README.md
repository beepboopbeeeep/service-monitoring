# 🚀 راهنمای کامل راه‌اندازی پنل مانیتورینگ

## 📋 پیش‌نیازها

1. یک حساب GitHub
2. ریپازیتوری عمومی (Public Repository)

## 🛠️ مراحل راه‌اندازی

### مرحله ۱: ایجاد ریپازیتوری جدید

1. به GitHub بروید
2. روی `+` کلیک کنید و `New repository` را انتخاب کنید
3. نام ریپازیتوری را وارد کنید (مثلاً `server-monitor`)
4. گزینه `Public` را انتخاب کنید
5. تیک `Add a README file` را بزنید
6. روی `Create repository` کلیک کنید

### مرحله ۲: آپلود فایل‌ها

1. تمام فایل‌های ایجاد شده را در ریپازیتوری آپلود کنید:
   - `index.html`
   - `app.js`
   - `monitor.py`
   - `status.json`
   - `history.json`
   - `.github/workflows/monitor.yml`
   - `README.md`

2. می‌توانید از طریق وب یا Git CLI این کار را انجام دهید

### مرحله ۳: فعال‌سازی GitHub Pages

1. به تنظیمات ریپازیتوری بروید (`Settings`)
2. از منوی سمت چپ `Pages` را انتخاب کنید
3. در بخش `Source`، `Deploy from a branch` را انتخاب کنید
4. در بخش `Branch`، `main` را انتخاب کنید و `/ (root)` را بزنید
5. روی `Save` کلیک کنید

### مرحله ۴: ویرایش تنظیمات

1. فایل `app.js` را باز کنید
2. خطوط ۸ و ۹ را ویرایش کنید:
   ```javascript
   statusUrl: 'https://raw.githubusercontent.com/YOUR_USERNAME/YOUR_REPO/main/status.json',
   historyUrl: 'https://raw.githubusercontent.com/YOUR_USERNAME/YOUR_REPO/main/history.json',
   ```
   - `YOUR_USERNAME` را با نام کاربری GitHub خود جایگزین کنید
   - `YOUR_REPO` را با نام ریپازیتوری خود جایگزین کنید

### مرحله ۵: فعال‌سازی GitHub Actions

1. به تب `Actions` بروید
2. روی `I understand my workflows, go ahead and enable them` کلیک کنید
3. کارflow به صورت خودکار فعال می‌شود

## ⚙️ تنظیمات پیشرفته

### اضافه کردن سرویس جدید

#### ۱. ویرایش `monitor.py`:
```python
{
    "id": "new-service",
    "name": "نام سرویس جدید",
    "url": "https://example.com/panel",
    "renewal_date": "1404/12/30",
    "color": "#2196F3"
}
```

#### ۲. ویرایش `app.js`:
```javascript
{
    id: 'new-service',
    name: 'نام سرویس جدید',
    url: 'https://example.com/panel',
    renewalDate: '1404/12/30',
    color: '#2196F3'
}
```

#### ۳. Commit و Push تغییرات

### تغییر تاریخ تمدید

1. فایل `monitor.py` را باز کنید
2. مقدار `renewal_date` را برای سرویس مورد نظر تغییر دهید
3. تغییرات را ذخیره و commit کنید

### تنظیم بازه مانیتورینگ

1. فایل `.github/workflows/monitor.yml` را باز کنید
2. خط `cron` را تغییر دهید:
   - هر ۵ دقیقه: `*/5 * * * *`
   - هر ۱۵ دقیقه: `*/15 * * * *`
   - هر ساعت: `0 * * * *`

## 📊 ویژگی‌های سیستم

### ✅ مانیتورینگ لحظه‌ای
- بررسی هر ۱۰ دقیقه
- تشخیص خودکار وضعیت‌ها
- ذخیره تاریخچه کامل

### 📈 نمودارهای پیشرفته
- نمودار هفتگی خطی
- نمودار ماهانه میله‌ای
- رنگ‌بندی خودکار

### 🎨 طراحی حرفه‌ای
- دارک مود کامل
- فونت وزیرمتن فارسی
- ریسپانسیو و واکنش‌گرا
- انیمیشن‌های زیبا

### ⚡ عملکرد بهینه
- لود سریع
- کش هوشمند
- به‌روزرسانی خودکار

## 🚨 عیب‌یابی

### مشکل: صفحه نمایش داده نمی‌شود
1. بررسی کنید GitHub Pages فعال باشد
2. URL را چک کنید: `https://username.github.io/repository-name`
3. چند دقیقه صبر کنید (اولین بار ممکن است طول بکشد)

### مشکل: مانیتورینگ کار نمی‌کند
1. به تب `Actions` بروید
2. لاگ اجرا را بررسی کنید
3. از صحت URL سرویس‌ها مطمئن شوید

### مشکل: نمودارها نمایش داده نمی‌شوند
1. کنسول مرورگر را باز کنید
2. خطاها را بررسی کنید
3. از دسترسی به فایل‌های JSON مطمئن شوید

## 🔐 امنیت

- تمام اتصالات با HTTPS
- هیچ داده حساسی ذخیره نمی‌شود
- دسترسی فقط خواندنی

## 📞 پشتیبانی

برای گزارش مشکل یا پیشنهاد:
1. Issue جدید در GitHub ایجاد کنید
2. مشکل را به طور کامل توضیح دهید
3. عکس‌ها و اطلاعات لازم را ضمیمه کنید

## 📄 مجوز

این پروژه تحت مجوز MIT منتشر شده است.

---

## 🎯 نکات مهم

1. **هرگز توکن‌های دسترسی را در کد قرار ندهید**
2. **برای سرویس‌های مهم از مانیتورینگ اضافی استفاده کنید**
3. **به‌طور منظم بک‌آپ بگیرید**
4. **تاریخ تمدیدها را به موقع به‌روز کنید**

## 🌟 ویژگی‌های منحصر به فرد

- نمایش تاریخ شمسی
- محاسبه خودکار روزهای باقی‌مانده
- رنگ‌بندی هوشمند بر اساس تاریخ تمدید
- سیستم اطلاع‌رسانی بصری
- پشتیبانی کامل از زبان فارسی
- سازگاری با تمام مرورگرها

## 🔄 به‌روزرسانی

سیستم به طور خودکار به‌روز می‌شود. برای به‌روزرسانی دستی:

1. تغییرات مورد نظر را اعمال کنید
2. Commit و Push کنید
3. منتظر اجرای GitHub Actions بمانید

---

**توسعه داده شده با ❤️ برای جامعه فارسی‌زبان**